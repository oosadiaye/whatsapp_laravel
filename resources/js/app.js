import './bootstrap';

// Alpine.js is bundled with Livewire v3, no need to import separately.
// Breeze scaffolded an Alpine import here but Livewire's bundle already provides it,
// which was causing "Detected multiple instances of Alpine running" warnings.
